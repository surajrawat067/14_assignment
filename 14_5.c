#include<stdio.h>
int main ()
{
    int G=9999,c, great[10];
    for(int i=0; i<=9; i++)
    {
        scanf("%d",&great[0]);
        if(great[0]<=G)
      { c= great[0];
       G= great[0];}
    }
    printf("%d is the smallest",c);
    return 0;
}